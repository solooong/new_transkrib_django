# Пакет настроек проекта (config.settings, config.urls, config.wsgi).
# Файл обязателен: без него каталог config не импортируется как пакет
# и Django падает с ModuleNotFoundError: No module named 'config.settings'.
